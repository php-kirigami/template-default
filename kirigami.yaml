# yaml-language-server: $schema=https://cdn.jsdelivr.net/gh/php-kirigami/kirigami@main/packages/kirigami/kirigami.schema.json

kirigami:
  project:  Kirigami Site
  baseurl:  https://example.github.io/kirigami-site
  root:     src
  banner:   banner.txt

  # Any other key here is free-form project data: it becomes a PHP variable of
  # the same name on every page and in the header / footer includes.
  author:      Your Name
  email:       you@example.com
  description: A static site built with Kirigami.
  tagline:     Built with Kirigami.


# SEO / social metadata. This block opts @kirigami/php-prepros' META class in;
# an empty map is enough — <title>, description, Open Graph, Twitter Card,
# canonical, favicon and apple-touch-icon are derived from the `kirigami:` keys
# above and each page's PHPDOC. Tags the layout already hand-writes are skipped.
# Drop favicon.ico / apple-touch-icon.png in src/ and they're picked up.
# Nest a `jsonld:` sub-block here too for schema.org JSON-LD — its own
# independent opt-in (see php-prepros' README).
seo: {}


prepros:
  before: _layouts/header.php   # prepended to every page
  after:  _layouts/footer.php   # appended to every page
  format: true
  includes: [_lib/functions.php] # register custom tags / shortcodes / hooks here


sass:
  style: expanded


tasks:
  - name:  css-core
    type:  sass
    entry: styles/kirigami.core.scss

  - name:  js-core
    type:  esbuild
    entry: scripts/kirigami.core.js
